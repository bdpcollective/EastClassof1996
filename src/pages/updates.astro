---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Banner from '../components/Banner.astro';
import Footer from '../components/Footer.astro';
---

<BaseLayout
  title="Keep In Touch | Lincoln East High School Class of 1996"
  description="Stay connected with the Lincoln East High School Class of 1996. Sign up to keep in touch and hear about future gatherings."
>
  <Header />
  <Banner />

  <main id="main-content">
    <section class="rsvp-page ambient-bg">
      <div class="container">
        <div class="rsvp-header animate-on-scroll">
          <h1>Keep In Touch</h1>
          <p class="rsvp-subtitle">Stay connected and be the first to hear about future gatherings</p>
          <div class="event-details">
            <div class="detail-item">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
              <span>Friday - Sunday, September 4-6, 2026</span>
            </div>
            <div class="detail-item">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
              <span>Lincoln, Nebraska</span>
            </div>
          </div>
        </div>

        <div class="rsvp-form-container">
          <form id="registerForm" class="rsvp-form">
            <div class="form-section">
              <h2>Personal Information</h2>
              <div class="form-row">
                <div class="form-group">
                  <label for="firstName">First Name *</label>
                  <input type="text" id="firstName" name="firstName" required>
                </div>
                <div class="form-group">
                  <label for="lastName">Last Name *</label>
                  <input type="text" id="lastName" name="lastName" required>
                </div>
              </div>

              <div class="form-row">
                <div class="form-group">
                  <label for="email">Email Address *</label>
                  <input type="email" id="email" name="email" required pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$" title="Please enter a valid email address">
                </div>
                <div class="form-group">
                  <label for="phone">Phone Number</label>
                  <input type="tel" id="phone" name="phone" pattern="[0-9]{3}-?[0-9]{3}-?[0-9]{4}|\([0-9]{3}\) [0-9]{3}-[0-9]{4}|[0-9]{10}" title="Please enter a valid phone number (e.g., 555-123-4567)" placeholder="555-123-4567">
                </div>
              </div>

              <div class="form-group">
                <label for="maidenName">Maiden Name (if applicable)</label>
                <input type="text" id="maidenName" name="maidenName">
              </div>
            </div>

            <div class="form-section">
              <h2>Comments or Messages</h2>
              <div class="form-group">
                <label for="comments">Share any memories, excitement, or thoughts...</label>
                <textarea id="comments" name="comments" placeholder="Share any memories, excitement, or thoughts..."></textarea>
              </div>
            </div>

            <div class="form-actions">
              <button type="submit" class="submit-btn">
                <span class="btn-text">Keep Me Updated</span>
                <span class="btn-loading">Submitting...</span>
              </button>
            </div>
          </form>

          <!-- Success Message -->
          <div id="successMessage" class="success-message" role="alert" aria-live="assertive">
            <div class="success-content">
              <div class="success-icon">&#10003;</div>
              <h2>You're on the list!</h2>
              <p>Thanks for staying connected. We'll keep you updated on future Class of '96 gatherings. See you in 10 years!</p>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>

  <Footer />

  <script>
    const GOOGLE_SHEETS_URL = 'https://script.google.com/macros/s/AKfycbz2faLu_MjmXEs3uVHHtRYS5xyRJzpnLFTOrZnju-4CF_KiBAjHbO1zkvfjiqnghhGKcA/exec';

    function submitToGoogleSheets(formData) {
      return fetch(GOOGLE_SHEETS_URL, {
        method: 'POST',
        mode: 'cors',
        headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
        body: new URLSearchParams(formData)
      });
    }

    function initRegisterForm() {
      const form = document.getElementById('registerForm');
      const submitBtn = form.querySelector('.submit-btn');
      const successMessage = document.getElementById('successMessage');

      form.addEventListener('submit', async (e) => {
        e.preventDefault();
        submitBtn.classList.add('loading');

        const formData = new FormData(form);

        const data = {
          firstName: formData.get('firstName'),
          lastName: formData.get('lastName'),
          email: formData.get('email'),
          phone: formData.get('phone'),
          maidenName: formData.get('maidenName'),
          comments: formData.get('comments'),
          source: 'updates',
          timestamp: new Date().toLocaleString()
        };

        try {
          await submitToGoogleSheets(data);
          form.style.display = 'none';
          successMessage.classList.add('active');
          createConfetti();
        } catch (error) {
          console.error('Error submitting form:', error);
          alert('There was an error submitting your information. Please try again.');
        } finally {
          submitBtn.classList.remove('loading');
        }
      });
    }

    function createConfetti() {
      const colors = ['#1e2a78', '#3b82f6', '#06b6d4', '#93c5fd', '#bfdbfe', '#ffffff'];
      for (let i = 0; i < 100; i++) {
        const confetti = document.createElement('div');
        confetti.className = 'confetti';
        confetti.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
        confetti.style.left = Math.random() * 100 + '%';
        confetti.style.animationDelay = Math.random() * 3 + 's';
        confetti.style.animationDuration = (Math.random() * 2 + 1) + 's';
        document.body.appendChild(confetti);
        setTimeout(() => confetti.remove(), 4000);
      }
    }

    document.addEventListener('DOMContentLoaded', initRegisterForm);
  </script>
</BaseLayout>

<style>

  .rsvp-page {
    padding: var(--section-padding) 0;
    background: var(--gradient-primary);
    min-height: 80vh;
  }

  .rsvp-page::before {
    z-index: 0;
  }

  .container {
    max-width: 800px;
    margin: 0 auto;
    padding: 0 20px;
    position: relative;
    z-index: 1;
  }

  .rsvp-header {
    text-align: center;
    margin-bottom: 60px;
  }

  .rsvp-header h1 {
    font-family: var(--font-display);
    font-size: 48px;
    font-weight: 600;
    color: white;
    margin: 0 0 20px 0;
    text-transform: uppercase;
    letter-spacing: 2px;
  }

  .rsvp-subtitle {
    font-family: var(--font-body);
    font-size: 20px;
    font-weight: 300;
    color: rgba(255, 255, 255, 0.9);
    margin: 0 0 40px 0;
  }

  .event-details {
    display: flex;
    justify-content: center;
    gap: 40px;
    flex-wrap: wrap;
  }

  .detail-item {
    display: flex;
    align-items: center;
    gap: 10px;
    font-family: var(--font-body);
    color: white;
    font-weight: 500;
  }

  .detail-item svg {
    color: var(--color-electric-light);
  }

  .rsvp-form-container {
    position: relative;
    z-index: 10;
  }

  .rsvp-form {
    background: white;
    border-radius: 16px;
    padding: 40px;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
    position: relative;
    z-index: 10;
  }

  .form-section {
    margin-bottom: 40px;
  }

  .form-section h2 {
    font-family: var(--font-display);
    font-size: 24px;
    font-weight: 600;
    color: var(--color-primary-700);
    margin: 0 0 20px 0;
    border-bottom: 2px solid var(--color-gray-100);
    padding-bottom: 10px;
  }

  .form-row {
    display: flex;
    gap: 20px;
  }

  .form-group {
    flex: 1;
    margin-bottom: 20px;
  }

  .form-group label {
    display: block;
    font-family: var(--font-body);
    font-weight: 500;
    color: var(--color-gray-700);
    margin-bottom: 8px;
    font-size: 14px;
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    width: 100%;
    padding: 12px 16px;
    border: 2px solid var(--color-gray-300);
    border-radius: 10px;
    font-family: var(--font-body);
    font-size: 16px;
    transition: all var(--transition-fast);
    color: var(--color-gray-700);
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--color-electric);
    box-shadow: 0 0 0 3px rgba(6, 182, 212, 0.15);
  }

  .form-group textarea {
    resize: vertical;
    min-height: 100px;
  }

  .form-actions {
    text-align: center;
    margin-top: 40px;
  }

  .submit-btn {
    background: var(--gradient-primary);
    color: white;
    border: 2px solid transparent;
    padding: 16px 32px;
    font-family: var(--font-display);
    font-size: 16px;
    font-weight: 600;
    border-radius: 12px;
    cursor: pointer;
    transition: all var(--transition-medium);
    position: relative;
    overflow: hidden;
    text-transform: uppercase;
    letter-spacing: 1px;
    box-shadow: 0 4px 15px rgba(6, 182, 212, 0.2);
  }

  .submit-btn:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 30px rgba(6, 182, 212, 0.3);
  }

  .submit-btn.loading .btn-text { opacity: 0; }
  .submit-btn.loading .btn-loading { opacity: 1; }

  .btn-loading {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .success-message {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: white;
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transform: scale(0.9);
    transition: all 0.3s ease;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
    pointer-events: none;
    z-index: 20;
  }

  .success-message.active {
    opacity: 1;
    transform: scale(1);
    pointer-events: auto;
  }

  .success-content {
    text-align: center;
    padding: 40px;
  }

  .success-icon {
    width: 80px;
    height: 80px;
    background: var(--gradient-primary);
    color: white;
    font-size: 2.5rem;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 20px;
    animation: bounce 2s infinite;
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
    40% { transform: translateY(-20px); }
    60% { transform: translateY(-10px); }
  }

  .success-content h2 {
    font-family: var(--font-display);
    font-size: 28px;
    color: var(--color-primary-700);
    margin: 0 0 15px 0;
  }

  .success-content p {
    font-family: var(--font-body);
    font-size: 16px;
    color: var(--color-gray-500);
    margin: 0 0 30px 0;
  }

  /* Confetti */
  :global(.confetti) {
    position: fixed;
    top: -10px;
    width: 10px;
    height: 10px;
    z-index: 1000;
    animation: confetti-fall linear forwards;
  }

  @keyframes confetti-fall {
    to {
      transform: translateY(100vh) rotate(360deg);
    }
  }

  @media (max-width: 768px) {
    .rsvp-header h1 { font-size: 32px; }
    .event-details { flex-direction: column; align-items: center; gap: 20px; }
    .rsvp-form { padding: 30px 20px; }
    .form-row { flex-direction: column; gap: 0; }
    .submit-btn { width: 100%; }
  }

  @media (max-width: 480px) {
    .rsvp-page { padding: 40px 0; }
    .rsvp-header h1 { font-size: 24px; }
    .rsvp-subtitle { font-size: 16px; }
  }
</style>
